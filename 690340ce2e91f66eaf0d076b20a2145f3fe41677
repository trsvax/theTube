Session: publish workflow + multi-agent notes. Created docs/notes.md — git notes convention so multiple AIs write consistent session journals. Replaced draft boolean with workflow: draft|published field across all 99 posts + build scripts. Fixed content.json → index.json rename in PostList, verify skill, docs. Fixed generateStaticParams to build all pages (drafts get URLs, just not in feeds). Wrote publish-workflow spec: all content under /{role}/posts/slug, UUID on comment-enabled posts, alias on rename. Wrote ai-lost-my-homework journal entry. Next: review post workflow statuses, implement role-gated HTML paths, comments.

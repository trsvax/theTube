Session: share system end-to-end capture. Deployed cf-share.js as a per-app CF function on /w/share/* behavior. Design decision: /w/ is the app mount point, each app owns its CF function and routing. Auth header is part of the path contract (no header → 404, path doesn't exist without it). Tested capture from Mac via share-request.sh → 202, verified in CF logs. Added sync_captures tool to MCP proxy — reads S3 logs, extracts /w/share/ entries, stores to SQLite, tracks processed files for idempotency. Fixed node:sqlite API (DatabaseSync, not Database). Wrote checkpoint journal (share-system-checkpoint.md) with 5 open questions: who writes src back, S3 path for images, Lambda origin routing, dedup on same-file captures, EC2 timing. Key insights: the AI is the UI (sync → query → draft blocks), per-app functions not shared, body-means-compute is a technical constraint not a feature, EC2 box is author tooling not site infrastructure. Next: iOS Shortcut, [share]: block renderer, Lambda for publish.

name: Aggregate site.json

on:
  workflow_dispatch:

jobs:
  aggregate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Generate site.json
        run: node scripts/aggregate-site.mjs

      - name: Configure AWS
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Sync site.json to S3
        run: aws s3 cp site.json s3://${{ secrets.S3_BUCKET }}/site.json

      - name: Commit site.json
        run: |
          git config user.email "actions@github.com"
          git config user.name "GitHub Actions"
          git add site.json
          git diff --staged --quiet || git commit -m "release: update site.json" && git push

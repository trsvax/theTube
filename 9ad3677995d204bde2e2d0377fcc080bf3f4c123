Session: /tube/ protocol design refinement. Settled the trust model: JWT present → Lambda verifies and decides; no JWT → 404; anonymous JWT for public-style captures. Clarified ? convention: data location only (URL vs body), can coexist, not a compute gate. Input compute ceiling is fixed: verify JWT + save body, nothing more. Output: Lambda serves Location. Plain JWT = lower trust tier (e.g. curl /tube/contact to introduce yourself). Renamed 202 Accepted → 202 Noted across code and specs. Fixed missed /w/comment → /tube/comment in theTube-comments. Updated docs across all repos to match. Next: [share]: block renderer in lib/posts.ts (tasks.md Phase 1, Tasks 1-4).

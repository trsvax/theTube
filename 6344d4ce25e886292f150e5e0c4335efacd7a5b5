Session: /w/ → /tube/ rename. Renamed the write endpoint across all repos — theTube, theTube-content, theTube-share, theTube-mcp, theTube-comments. Code (cf-short-urls.js, cf-share.js, build-cf-function.mjs, proxy.js, share-request.sh, hooks.ts, Comments.tsx), docs (routing.md, filesystem.md), specs (events-endpoint, share, platform), posts (9 files), DESIGN.md. the-w-protocol.md deleted, replaced with the-tube-protocol.md. Clean cutover, no redirect. One remaining infra step: rename CloudFront behavior /w/share/* → /tube/share/* in AWS console.

Session: comment system design. Designed the full comment architecture — /w/comment/open for tokens, /w/comment/add for writes, processor Lambda from logs, index.json as readdir(). Key decisions: request IDs as filenames (no coordination), open() as privilege escalation (trust levels in token), ? as security boundary (batch vs realtime), single writer via log processing, index Lambda as future upgrade. Renamed site.json/content.json to index.json everywhere (index.json is ls). Wrote POSIX filesystem mapping doc. Built reference client (React, useQuery/useMutate) and processor Lambda. The platform is an OS: CDN is kernel, S3 is filesystem, Lambda is userspace, logs are the journal. Next: wire infrastructure (CloudFront Function, deploy Lambda, WAF rule).

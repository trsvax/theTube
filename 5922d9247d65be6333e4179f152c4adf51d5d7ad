Session 2026-05-20:

Key ideas:
- Client picks comment type based on role (JWT = routing hint + enforcement)
- Two-layer auth: CloudFront Function (no JWT → 403) + Lambda@Edge (validates signature/role)
- 202 = committed. The response is the commit acknowledgment.
- useQuery is just fetch with ergonomics — same URL, progressive enhancement
- 9P at internet scale: HTTPS is 9P, S3+git is the file server, JWT is the namespace, Lambda is cpu
- Statelessness isn't a compromise from 9P — it's an improvement
- Duck filesystem (DFS taken, theirs is named wrong)
- QED status for journal entries — proof complete, implementation optional (□)
- GitHub as CMS: GitHub App + Contents API + static frontend = WordPress without WordPress

Posts created:
- 9p-at-internet-scale
- github-as-cms (first qed status)
- cleanup (running backlog)

Posts updated:
- same-hooks-no-server (role routing, useQuery is just fetch)
- realtime-comments (two-layer enforcement, client picks path)
- moderated-comments (grep not athena)

Other:
- workflow.md steering: links to thetube.today not github, minimal format after push
- Added agentic last mile identity video to links
